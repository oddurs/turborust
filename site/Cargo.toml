[workspace]

[package]
name = "site"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pulldown-cmark = { version = "0.12", default-features = false, features = ["html"] }

[profile.dev]
debug = "line-tables-only"
