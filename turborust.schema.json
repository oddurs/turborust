{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "type": "object",
  "properties": {
    "cache": {
      "$ref": "#/$defs/SharedCache"
    },
    "overlay": {
      "$ref": "#/$defs/Overlay"
    },
    "project": {
      "$ref": "#/$defs/Project"
    },
    "services": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/Service"
      }
    },
    "tasks": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/Task"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "CachePolicy": {
      "type": "string",
      "enum": [
        "enabled",
        "disabled"
      ]
    },
    "EnvMode": {
      "description": "How much of the ambient environment a task's command may see.\n\nThe default is `strict`, and that is a correctness property rather than a\npreference: the cache key can only account for variables it knows about, so\nthe only way to guarantee an undeclared variable did not change the build is\nto make sure the build never saw it.",
      "oneOf": [
        {
          "description": "Only the safe base, `env_keys`, `pass_through_env` and inline `env`.",
          "type": "string",
          "const": "strict"
        },
        {
          "description": "Inherit the full ambient environment — and therefore never cache.",
          "type": "string",
          "const": "loose"
        }
      ]
    },
    "ErrorMode": {
      "description": "What a failed build does to the page.",
      "oneOf": [
        {
          "description": "Take over the viewport, the way a compile error deserves.",
          "type": "string",
          "const": "overlay"
        },
        {
          "description": "Only mark the bubble; stay out of the way.",
          "type": "string",
          "const": "badge"
        },
        {
          "description": "Show nothing.",
          "type": "string",
          "const": "silent"
        }
      ]
    },
    "Health": {
      "type": "object",
      "properties": {
        "http": {
          "description": "HTTP GET; any 2xx/3xx counts as healthy.",
          "type": [
            "string",
            "null"
          ]
        },
        "interval": {
          "type": "string",
          "default": "300ms"
        },
        "log": {
          "description": "Substring the process must print before it counts as ready.\n\nFor the many dev processes that announce themselves on stdout and have no\nport worth probing — `trunk`, `cargo leptos watch`, migration tools.\nMatched against ANSI-stripped output.",
          "type": [
            "string",
            "null"
          ]
        },
        "tcp": {
          "description": "TCP connect probe against this port.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "OnChange": {
      "description": "What a watched-file change does to a running service.",
      "oneOf": [
        {
          "description": "Stop and start again, interrupting a startup already in progress.",
          "type": "string",
          "const": "restart"
        },
        {
          "description": "Let a startup finish before acting, so a cold build is never killed\nhalfway and then started from scratch.",
          "type": "string",
          "const": "queue"
        },
        {
          "description": "Send `signal` instead of restarting — for processes that reload\ntheir own configuration.",
          "type": "string",
          "const": "signal"
        },
        {
          "description": "Do nothing.",
          "type": "string",
          "const": "ignore"
        }
      ]
    },
    "Overlay": {
      "description": "The in-browser dev overlay.\n\nDeliberately shaped like Turbopack's `devIndicators`: one table, sensible\ndefaults, and every value also overridable at runtime from the overlay's own\npreferences pane — so moving the bubble out of the way of your own UI never\nrequires editing a file and restarting.",
      "type": "object",
      "properties": {
        "emoji": {
          "description": "The face in the bubble. Any emoji; the crab is just the default.",
          "type": "string",
          "default": "🦀"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "errors": {
          "$ref": "#/$defs/ErrorMode"
        },
        "position": {
          "$ref": "#/$defs/Position"
        },
        "shortcut": {
          "description": "Toggles the panel. Parsed in the browser, not here.",
          "type": "string",
          "default": "ctrl+`"
        },
        "theme": {
          "$ref": "#/$defs/Theme"
        }
      },
      "additionalProperties": false
    },
    "Position": {
      "type": "string",
      "enum": [
        "bottom-right",
        "bottom-left",
        "top-right",
        "top-left"
      ]
    },
    "Project": {
      "type": "object",
      "properties": {
        "concurrency": {
          "description": "How many nodes may be building at once. Defaults to the machine's\nparallelism, capped, because builds are memory-hungry rather than\nmerely CPU-hungry.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "ignore": {
          "description": "Globs excluded from every watcher and every input hash.",
          "type": "array",
          "default": [
            "**/.git/**",
            "**/target/**",
            "**/node_modules/**",
            "**/.turborust/**",
            "**/dist/**",
            "**/.next/**"
          ],
          "items": {
            "type": "string"
          }
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "notify": {
          "description": "Show a desktop notification when a node fails or recovers.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "ProxyRule": {
      "description": "One forwarding rule for the static server.\n\nThe point is a single origin: a page served here can call `/api/...` with a\nrelative path, so there is no CORS to configure in development and no API\norigin to swap out for production.",
      "type": "object",
      "properties": {
        "path": {
          "description": "Path prefix to match, e.g. `/api`.",
          "type": "string"
        },
        "strip_prefix": {
          "description": "Remove the matched prefix before forwarding, for a backend that does not\nknow it is mounted under one.",
          "type": "boolean",
          "default": false
        },
        "to": {
          "description": "Origin to forward to, e.g. `http://127.0.0.1:8788`.",
          "type": "string"
        },
        "websocket": {
          "description": "Proxy WebSocket upgrades on this prefix.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "path",
        "to"
      ]
    },
    "RestartPolicy": {
      "oneOf": [
        {
          "description": "Restart on crash and on watched-file change.",
          "type": "string",
          "const": "always"
        },
        {
          "description": "Restart only when watched files change; a clean exit stays exited.",
          "type": "string",
          "const": "on-change"
        },
        {
          "description": "Never restarted automatically.",
          "type": "string",
          "const": "never"
        }
      ]
    },
    "Serve": {
      "description": "Static file serving for a wasm/SPA frontend, with browser live-reload.",
      "type": "object",
      "properties": {
        "allow_remote_control": {
          "description": "Permit the mutating endpoints (restart) when bound beyond loopback.\n\nOff by default and separate from `host` on purpose: wanting to test on a\nphone is not consent to let the network restart your processes.",
          "type": "boolean",
          "default": false
        },
        "dir": {
          "description": "Directory of built assets, relative to the workspace root.",
          "type": "string"
        },
        "host": {
          "description": "Address to bind. Defaults to loopback.\n\nBinding beyond loopback is what lets you open the page on a phone, and it\nalso makes every endpoint reachable by anything on the network — see\n`allow_remote_control`.",
          "type": "string",
          "default": "127.0.0.1"
        },
        "live_reload": {
          "description": "Inject a reload client into served HTML and push reloads on rebuild.",
          "type": "boolean",
          "default": true
        },
        "open": {
          "description": "Open a browser once the server is ready.",
          "type": "boolean",
          "default": false
        },
        "port": {
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "spa": {
          "description": "Fall back to index.html for unknown paths (client-side routing).",
          "type": "boolean",
          "default": true
        },
        "tls": {
          "description": "Serve over HTTPS with this certificate and key.",
          "anyOf": [
            {
              "$ref": "#/$defs/Tls"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "dir",
        "port"
      ]
    },
    "Service": {
      "type": "object",
      "properties": {
        "backoff_max": {
          "type": "string",
          "default": "30s"
        },
        "backoff_min": {
          "type": "string",
          "default": "250ms"
        },
        "cargo": {
          "description": "Cargo package name. Derives the run command, the watch set, and the\nrebuild edges from `cargo metadata` — including transitive path deps,\nwhich is the part hand-written globs always get wrong.",
          "type": [
            "string",
            "null"
          ]
        },
        "cargo_args": {
          "description": "Extra args appended to the derived cargo command.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "cmd": {
          "description": "Command to run. Optional when `cargo` or `serve` supplies one.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "cwd": {
          "type": [
            "string",
            "null"
          ]
        },
        "debounce": {
          "description": "Debounce window for watch events before acting.",
          "type": "string",
          "default": "120ms"
        },
        "depends_on": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "health": {
          "anyOf": [
            {
              "$ref": "#/$defs/Health"
            },
            {
              "type": "null"
            }
          ]
        },
        "ignore": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "on_change": {
          "description": "What a watched-file change does to this service.",
          "$ref": "#/$defs/OnChange"
        },
        "port": {
          "description": "Reclaimed before start and checked for readiness if no explicit health probe.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "proxy": {
          "description": "Forward matching paths to another origin, so the whole stack is one origin.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ProxyRule"
          }
        },
        "ready_timeout": {
          "type": "string",
          "default": "60s"
        },
        "restart": {
          "$ref": "#/$defs/RestartPolicy"
        },
        "serve": {
          "description": "Serve a directory over HTTP with live-reload injection (wasm frontends).",
          "anyOf": [
            {
              "$ref": "#/$defs/Serve"
            },
            {
              "type": "null"
            }
          ]
        },
        "signal": {
          "description": "Signal sent when `on_change = \"signal\"`, e.g. \"SIGHUP\".",
          "type": [
            "string",
            "null"
          ]
        },
        "stop_timeout": {
          "type": "string",
          "default": "8s"
        },
        "target_dir": {
          "$ref": "#/$defs/TargetDir"
        },
        "watch": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "SharedCache": {
      "description": "A second cache directory, shared with other machines or checkouts.\n\nThe trust model is the whole design, so it is stated here rather than in a\ndoc comment nobody reads: a build cache maps inputs to **outputs**, so anyone\nwho can write to a cache you read from can hand your build arbitrary\nartifacts. Reading is a trust decision; writing is a bigger one.\n\nTherefore `push` defaults to false. Pointing at a shared directory gets you\nits results; contributing yours is a separate, explicit choice.",
      "type": "object",
      "properties": {
        "dir": {
          "description": "Where to keep results. Defaults to a per-user store shared by every\nworktree of this repository, because results are content-addressed and\ntherefore portable between them.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_size": {
          "description": "Byte budget for the local store: `10GiB`, `500MB`, or `0` to keep\neverything. Least-recently-*used* results are dropped first.\n\nDeliberately not applied to a shared store. A shared cache is not this\nmachine's to garbage-collect — evicting from it would delete results\nother people are still reading, on the strength of one machine's budget.",
          "type": "string",
          "default": "10GiB"
        },
        "push": {
          "description": "Also write this machine's results there. Off unless asked for.",
          "type": "boolean",
          "default": false
        },
        "shared": {
          "description": "Directory to read results from, in addition to the local cache. A network\nmount, a synced folder, or another checkout.",
          "type": [
            "string",
            "null"
          ]
        },
        "verify": {
          "description": "Re-run some cache hits and check they really reproduce what was stored.",
          "$ref": "#/$defs/VerifyMode"
        }
      },
      "additionalProperties": false
    },
    "TargetDir": {
      "description": "Where a node's cargo builds put their artifacts.\n\ncargo takes an exclusive lock on its target directory, so two nodes building\nat once serialise. Splitting removes the contention and is a clear win when\nthe two builds share nothing anyway — a wasm frontend and a native backend\ncompile for different targets. It is usually a loss for two native binaries\nin one workspace, because every shared dependency is then compiled twice and\nstored twice.",
      "oneOf": [
        {
          "description": "The workspace default. Shared, and therefore serialised under contention.",
          "type": "string",
          "const": "shared"
        },
        {
          "description": "A private target directory for this node.",
          "type": "string",
          "const": "split"
        }
      ]
    },
    "Task": {
      "type": "object",
      "properties": {
        "cache": {
          "$ref": "#/$defs/CachePolicy"
        },
        "cargo": {
          "description": "Cargo package name; auto-derives `inputs` from the crate and its path deps.",
          "type": [
            "string",
            "null"
          ]
        },
        "cargo_args": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "cmd": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "cwd": {
          "type": [
            "string",
            "null"
          ]
        },
        "depends_on": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "env_keys": {
          "description": "Env vars whose values are folded into the cache key.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "env_mode": {
          "$ref": "#/$defs/EnvMode"
        },
        "ignore": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "inputs": {
          "description": "Globs whose content hash forms the cache key. Empty inputs = never cached.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "outputs": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "pass_through_env": {
          "description": "Env vars passed through to the child but *not* folded into the key.\nThe escape hatch for things that must reach the build without keying it.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "target_dir": {
          "$ref": "#/$defs/TargetDir"
        }
      },
      "additionalProperties": false
    },
    "Theme": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "dark",
            "light"
          ]
        },
        {
          "description": "Follow the viewer's `prefers-color-scheme`.",
          "type": "string",
          "const": "auto"
        }
      ]
    },
    "Tls": {
      "type": "object",
      "properties": {
        "cert": {
          "description": "PEM certificate chain.",
          "type": "string"
        },
        "key": {
          "description": "PEM private key.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "cert",
        "key"
      ]
    },
    "VerifyMode": {
      "description": "How much of the cache to check against reality as you go.\n\n`turborust verify` answers the question deliberately; this answers it during\nordinary work, which is when a cache actually starts lying to you.",
      "oneOf": [
        {
          "description": "Trust the cache. The default: checking costs a rebuild.",
          "type": "string",
          "const": "off"
        },
        {
          "description": "Check one hit in [`VERIFY_SAMPLE_RATE`].",
          "type": "string",
          "const": "sample"
        },
        {
          "description": "Check every hit. Correct, and roughly as slow as having no cache.",
          "type": "string",
          "const": "always"
        }
      ]
    }
  }
}
