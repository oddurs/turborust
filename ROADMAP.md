# Roadmap

turborust is a dev orchestrator for full-Rust stacks: one binary that supervises
your services, watches the right files, caches your builds, and tells you exactly
why anything re-ran.

**How to read this.** `v0.0` is what already exists, recorded after the fact so
this file reflects reality rather than intentions — including the bugs found while
building it, which are kept because each note is the cheapest way to stop the bug
being reintroduced. `v0.1` is what has to be true before this is worth dogfooding.
`v1.0` is what has to be true before anyone else should depend on it.

**Where it stands.** Every milestone is closed. The runner works end to end — a
change to a shared crate restarts the backend, the browser reloads, and `turborust
why` names the file and the hash that caused it. The cache is trustworthy: a strict
filtered environment means an undeclared variable cannot change a build without
changing its key, a hit replays archived outputs rather than assuming the tree was
untouched, and the toolchain and config participate in every key. Results can be
shared between machines, read-only by default.

The supervision tree is covered by an integration harness rather than by a shell
script, which is the change that stops the next race being found by luck.

Two things are deliberately *not* claimed. The Windows process-tree teardown is
written and compile-checked against the real target but has never been run; CI
covers it on `windows-latest` and that is where it will first be exercised. And
`0023` records why hot patching stays out of scope: it is a binary-patching
problem, not an orchestration one.

**Out of scope, on purpose.** Five things comparable tools ship that this one will
not: asset pipelines (`0055`), toolchain installation (`0056`), codegen /
codeowners / docker / VCS hooks (`0057`), telemetry (`0058`), and a background
daemon (`0059`). Each is filed as a dropped item with its reasoning, so the
decision is discoverable rather than folklore. The test applied throughout: does
the feature need the dependency graph or the fingerprint? If not, it belongs in a
different tool.

**Provenance.** Items `0014`–`0023` came from an audit against watchexec,
turborepo, moon, cargo-leptos, trunk, dioxus, overmind and salsa — read for design
only, never copied. `bacon` was deliberately excluded: it is AGPL-3.0, and reading
copyleft source before writing adjacent code into a permissive project is a
contamination risk. See `0022`.

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## v0.0 — The runner itself

`##########` 100% · 15 of 15 done · due 2026-09-08

Everything that already exists: the supervision tree, the cache, the watcher, and both UIs.

### done

- [x] `0004` run <task> silently skips service dependencies <sup>bug · p0</sup>
- [x] `0025` Supervision tree with a readiness cascade <sup>feature</sup>
- [x] `0026` PTY-backed process supervision <sup>feature</sup>
- [x] `0027` Content-addressed task cache and turborust why <sup>feature</sup>
- [x] `0028` Derive watch and input globs from the cargo dependency closure <sup>feature</sup>
- [x] `0029` Workspace file watcher with debounced dispatch <sup>feature</sup>
- [x] `0030` Terminal UI <sup>feature</sup>
- [x] `0031` Static server with live reload for wasm frontends <sup>feature</sup>
- [x] `0032` Browser dev overlay <sup>feature</sup>
- [x] `0033` turborust doctor <sup>feature</sup>
- [x] `0034` Watching died silently when the watcher moved into a spawned task <sup>bug</sup>
- [x] `0035` macOS FSEvents reported a directory, so no glob matched <sup>bug</sup>
- [x] `0036` One edit restarted a service twice <sup>bug</sup>
- [x] `0037` The pty reader started after the child, losing fast output <sup>bug</sup>
- [x] `0038` A task could run twice from a stale dependency wake-up <sup>bug</sup>

## v0.1 — A dev loop you can trust

`##########` 100% · 7 of 7 done · due 2026-12-01

Enough to dogfood in a real project.

### done

- [x] `0005` Readiness probe that matches a log line <sup>feature · p1</sup>
- [x] `0006` Integration harness for the supervisor <sup>chore · p1</sup>
- [x] `0007` Shutdown polls instead of awaiting supervisors <sup>chore · p2</sup>
- [x] `0014` Undeclared env vars produce wrong cache hits <sup>bug · p0</sup>
- [x] `0015` Outputs are not in the cache key and are never restored <sup>bug · p1</sup>
- [x] `0016` Control queue has no priority, so Stop can be starved <sup>bug · p1</sup>
- [x] `0022` turborust has no LICENSE <sup>chore · p1</sup>

## v1.1 — Someone else can use it

`########··` 80% · 4 of 5 done · due 2027-05-01

The gaps that are not polish, but holes in what is already claimed.

### in progress

- [ ] `0063` A fresh clone gets none of the workflow <sup>bug · p1</sup>

### done

- [x] `0041` turborust connect: attach to a running process <sup>feature · p0</sup>
- [x] `0042` Proxy: one origin for the whole stack <sup>feature · p0</sup>
- [x] `0043` Nothing bounds how many builds run at once <sup>bug · p1</sup>
- [x] `0060` up panics without a terminal instead of erroring <sup>bug · p1</sup>

## v1.2 — Fit for CI

`##########` 100% · 6 of 6 done · due 2027-07-01

Selection, prediction and machine-readable output, for a build server.

### done

- [x] `0044` Select what runs with --filter <sup>feature · p1</sup>
- [x] `0045` Run only what changed: --affected <sup>feature · p1</sup>
- [x] `0046` Predict a run: --dry-run and --summarize <sup>feature · p1</sup>
- [x] `0047` Output modes and log persistence <sup>feature · p2</sup>
- [x] `0048` Keep going after a failure: --continue <sup>feature · p2</sup>
- [x] `0049` Reverse dependency index on the plan <sup>chore · p1</sup>

## v1.0 — Safe to depend on

`##########` 100% · 9 of 9 done · due 2027-03-01

Documented, tested, and safe to depend on.

### done

- [x] `0008` Restart a process from the overlay <sup>feature · p2</sup>
- [x] `0009` Overlay resends the whole log tail on every frame <sup>chore · p2</sup>
- [x] `0010` Opt-in per-node CARGO_TARGET_DIR <sup>feature · p2</sup>
- [x] `0011` doctor --fix applies suggestions with consent <sup>feature · p3</sup>
- [x] `0017` No global hash: toolchain and config changes do not invalidate <sup>bug · p1</sup>
- [x] `0018` Reload always discards client state <sup>feature · p2</sup>
- [x] `0019` Adopt process-wrap for process grouping <sup>chore · p2</sup>
- [x] `0020` on-busy-update: restart is the only policy <sup>feature · p3</sup>
- [x] `0021` No passthrough args on run <sup>feature · p3</sup>

## v1.3 — A cache you can audit

`########··` 75% · 3 of 4 done · due 2027-09-01

The cache is the load-bearing claim in this project. `why` explains it, the

### backlog

- [ ] `0072` Sibling worktrees each start with a cold cache <sup>bug · p2</sup>

### done

- [x] `0069` Downstream rebuilds when an upstream re-runs with identical output <sup>bug · p1</sup>
- [x] `0070` The cache is bounded by count, and evicts what you use most <sup>bug · p1</sup>
- [x] `0071` Nothing proves a task is deterministic <sup>feature · p1</sup>

## v1.4 — The loop keeps up

`··········` 0% · 0 of 4 done · due 2027-11-01

The dev loop moved while this tool was being built.

### backlog

- [ ] `0073` A node that reloads itself is invisible to the supervision tree <sup>feature · p0</sup>
- [ ] `0074` Test failures are invisible where every other failure is visible <sup>feature · p1</sup>
- [ ] `0075` doctor advises but never measures <sup>feature · p1</sup>
- [ ] `0078` Machine-readable output is not a stable contract <sup>chore · p2</sup>

## later — Not now, not never

`#######···` 67% · 8 of 12 done

Good ideas without a date yet.

### backlog

- [ ] `0061` Dynamic completion for node names <sup>feature · p3</sup>
- [ ] `0062` pty output capture is unverified on Windows <sup>bug · p2</sup>
- [ ] `0076` Hash a node's inputs only when the graph needs its key <sup>chore · p3</sup>
- [ ] `0077` An MCP server, once the JSON contract is stable <sup>feature · p3</sup>

### done

- [x] `0012` Windows: stop() cannot signal a process group <sup>bug · p2</sup>
- [x] `0013` Shared build cache across machines <sup>feature · p3</sup>
- [x] `0023` Research: what Rust hot patching would actually cost <sup>docs · p3</sup>
- [x] `0050` Shell completions and a man page <sup>chore · p3</sup>
- [x] `0051` JSON schema for turborust.toml <sup>chore · p3</sup>
- [x] `0052` Serve over TLS, on the network, and open a browser <sup>feature · p3</sup>
- [x] `0053` turborust graph <sup>feature · p3</sup>
- [x] `0054` Notify on failure <sup>feature · p3</sup>

## Unscheduled

`#######···` 67% · 2 of 3 done

### backlog

- [ ] `0064` a site for turborust, built by turborust <sup>feature · p2</sup>

### done

- [x] `0065` site: redraw illustrations, new type system, fix layout bugs <sup>bug · p2</sup>
- [x] `0066` plan the next two milestones <sup>chore · p2</sup>

